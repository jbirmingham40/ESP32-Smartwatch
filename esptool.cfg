[esptool]
; Default is 3s — increase it
timeout = 20
serial_write_timeout = 20

; If your link drops mid-write, retry blocks more times
write_block_attempts = 10

; If getting into bootloader is flaky:
connect_attempts = 10